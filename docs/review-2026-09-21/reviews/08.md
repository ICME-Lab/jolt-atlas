# Document Atlas reviews, validation evidence and native CI coverage

The consolidated implementation PRs span several dependent branches and retain measurements from distinct experiments. This documentation adds a central review index, a map from all 67 original PRs to seven implementation groups, and the source identities and validation evidence needed to assess each claim.

**Topic** review documentation across the Atlas contribution groups. **Type** Documentation, CI coverage and two existing iterator lint repairs. **PR target** `main`. **Prerequisites** None for merging the documentation. The documented code follows #369 before #370/#371, then #371, #372, #373, #374 and #375 in order along the native path.

## Contents

- [Review index](https://github.com/ICME-Lab/jolt-atlas/blob/review/consolidation-records/docs/review-2026-09-21/README.md) and seven descriptions, with concrete changes, explicit benchmark controls, measurements and limitations.
- Original PR and commit maps, preserved source heads, component review order, and publication receipts.
- Exact Linux commands, logs and file identities, including ordinary proof parity across feature configurations and the known experimental ONNX ZK failures.
- History alignment records after the round-two squash merge, and SHA-256 checksums for the archived files.

## Review scope

The archive is under `docs/review-2026-09-21/` and includes small proof fixtures and scripts used to authenticate the recorded checks. This PR also updates `.github/workflows/rust.yml` to exercise native BlindFold and report the unfinished ONNX dispatcher separately. Two map iterations in `joltworks/src/poly/opening_proof.rs` use `values()`/`values_mut()` to satisfy strict Clippy; these equivalent iterator changes are already present in #369. No proof predicate or dependency changes. The branch can merge independently of the implementation PRs.

Performance numbers retain their individual baselines and workload boundaries. They are not a new benchmark of the combined branches, and gains from separate controls must not be multiplied. Dated CI snapshots are historical observations, not a claim that current CI is green. The known experimental ONNX dispatcher limitations remain explicit.

## Validation

- Confirmed that main is an ancestor and the PR adds only the documented CI and iterator changes outside the evidence archive.
- Verified the evidence manifest and refreshed checksums after updating the index and review descriptions.
- Confirmed that #374 and #375 target main at the previously validated code heads. Their Linux tests retain their original source and feature scope.
- Fresh Linux Rust 1.95.0 checks on this branch pass strict native all-target Clippy, 188 native ZK tests and one documentation test. The existing ignored documentation test remains unchanged. No new performance measurement is claimed.

## CI behavior

`Clippy & Test (zk)` runs strict all-target Clippy and tests for `joltworks --features zk`. The previous ONNX commands remain in **Experimental ONNX ZK (informational)**, with a complete test attempt, visible failures and an outcome summary. That job is non-blocking because the dispatcher lacks several proof relations. This is a CI scope change, not a completion of the dispatcher. No test is removed or ignored.

All ordinary workspace, formatting, model proof and WebAssembly checks remain in the main workflow. The checkout action is updated from its obsolete version. The workflow passes actionlint.
